# Reconstructed meshes are large data artifacts: keep them out of git; keepers
# are uploaded to external storage where needed. This file keeps
# data/reconstructions/ in the repo while ignoring every run's meshes written
# into it. Post-processing scripts live in data/ (one level up) and are tracked
# normally.
*
!.gitignore