# Generated CIR plots are derived artifacts, regenerable from data/bags/: keep
# them out of git. This file keeps data/plots/ in the repo while ignoring every
# plot written into it. Post-processing scripts live in data/ (one level up)
# and are tracked normally.
*
!.gitignore
